# AGENTS.md — {{ project_name }}

Codex は `AGENTS.md`、ディレクトリ別 `AGENTS.md`、`.agents/skills/`、
`.codex/config.toml`、`.codex/rules/` を読む。Claude Code 用の `CLAUDE.md`
と運用思想は共有するが、Codex では `@file` import や `.claude/rules/` に
依存しない。

{% include "harness/shared/partials/agent-body.md.j2" %}

{% if include_cookbook_rule -%}
## Codex 補助ルール

Claude Code では一部の補助ルールを `.claude/rules/` に分けている。
Codex はそのディレクトリを自動読み込みしないため、Codex でも必要な共有ルールは
この `AGENTS.md` に明示する。

{% if include_cookbook_rule -%}
{% set rule_heading = "###" -%}
{% include "harness/shared/rules/cookbook.md.j2" %}
{% endif -%}
{% endif -%}
