{{ rule_heading | default("#") }} runops へのフィードバック (upstream feedback)

このプロジェクトは `runops` (https://github.com/Nkzono99/runops) 上で
運用されている。Agent は日常運用の過程で runops 自体の改善点
(バグ・使いにくさ・不足している機能・分かりにくい docs / エラーメッセージ・
抜けている adapter / launcher サポート 等) に気づいたら、upstream に
フィードバックして runops を育てることもタスクに含める。
フィードバックの記録、routing、sanitize、export は HarnessOps (`hops`) に委譲し、
GitHub issue はサニタイズ済み bundle から作る外部共有チャンネルとして扱う。

{{ rule_heading | default("#") }}# いつフィードバックするか

- `runo <command>` がクラッシュした、または unhelpful なエラーで止まった
- コマンド体系や help が実態と噛み合っていない
- 「もう一段抽象化してあれば手作業が減る」ワークフローが繰り返し出てきた
- `CLAUDE.md` / skill / rule の記述と実装が食い違っている
- 新しい simulator / launcher / site を足したくなった
- runops package のコードや generated harness を読んでいて明らかなバグ・dead code を発見した

「この 1 回だけ回避できればよい」ではなく、**次に別のプロジェクトで同じ作業を
する人 (自分含む) が困らないようにする** という視点で判断する。

{{ rule_heading | default("#") }}# フィードバック手順

1. **HarnessOps overlay を確認する** — `hops doctor --check-overlay` を実行する。
   未初期化なら `hops detect` の結果を確認し、runops project では通常
   `hops init --profile runops-project --with-agent-bridge` で初期化してから進める。
   `.harnessops/` や `harness-feedback/` を手で組み替えない。
2. **失敗/摩擦を記録する** — issue 本文をいきなり書かず、まず private な
   HarnessOps record にする:

   ```bash
   hops add-failure --title "<短い題名>" --target runops \
     --context "<文脈>" \
     --what-happened "<起きたこと>" \
     --why-matters "<重要性>" \
     --desired-behavior "<望ましい挙動>" \
     --local-workaround "<回避策>"
   hops route --record F0001
   ```

3. **upstream bundle を作る** — route が `target-upstream-candidate` の場合だけ、
   runops への下書きとして export する:

   ```bash
   hops add-feedback --from F0001 --target runops --summary "<要約>"
   hops feedback export --target runops --sanitize --format github-issue
   ```

   export された `harness-feedback/views/exported-feedback/UF*.md` を読み、ローカルパス、
   private term、project 固有の研究判断が残っていないか確認する。
4. **まずユーザーに提案する** — issue を切る前に必ず「こういう点で
   runops に issue を立てようと思うがどうか」と相談する。勝手に issue を
   投げない。
5. ユーザーが OK したら、`gh` で重複 issue を検索し、
   `gh` を使って issue を作成する:

   ```bash
   gh issue list --repo Nkzono99/runops --search "<キーワード>" --state all --limit 10
   ```

   ```bash
   gh issue create \
     --repo Nkzono99/runops \
     --title "<短い要約>" \
     --body "$(cat <<'EOF'
   ## 概要
   <何が問題か / どんな改善を提案するか>

   ## 再現手順 (bug の場合)
   1. ...
   2. ...

   ## 期待する挙動
   <どうあるべきか>

   ## 実際の挙動
   <いま何が起きているか / エラーメッセージ>

   ## 環境
   - runops version: $(uvx --from runops runo --version 2>/dev/null || echo unknown)
   - OS: $(uname -srm)
   - simulator: <該当 adapter 名 / バージョン>
   - 関連 run: <run_id があれば>

   ## 補足
   <ログ抜粋, 関連ファイルパス, 既に試した workaround など>
   EOF
   )"
   ```

6. 投げた issue の URL を `notes/YYYY-MM-DD.md` (`runo notes append`) に
   記録しておく。後から「あのとき投げた issue」で辿れるようにする。

{{ rule_heading | default("#") }}# 軽微な修正は直接 PR でもよい

typo 修正や small な挙動改善は、project とは別の runops source checkout で
直接 upstream に投げられる:

1. runops repository を別ディレクトリに clone し、`git checkout -b fix/<short-name>`
2. 修正を入れて `uv run pytest` などでテストが通ることを確認
3. 必要なら project 側の再現環境で実挙動を確認
4. `git push origin fix/<short-name>` → `gh pr create --repo Nkzono99/runops`
5. 作成した PR の URL をユーザーに共有し、承認を求める

大きな変更 (API 変更, 新 adapter, ワークフロー変更) は PR の前に issue で
先に設計を合意する。

{{ rule_heading | default("#") }}# local patch から upstream へ戻す判断

current project で今すぐ必要な修正は、別の runops source checkout で検証し、
必要なら project の `.venv` へ一時的に package install して確認してよい。
その後、必ず upstream disposition を決める:

| 判定 | 意味 | 次の動き |
|------|------|----------|
| `local-only` | project 固有 | project 側に残す |
| `feedback-issue` | 一部汎用 / 設計が必要 / draft PR には早い | `feedback-runops` で HarnessOps record + issue 下書き |
| `draft-pr` | 実装案も見せたいが設計レビューが必要 | draft PR |
| `ready-pr` | 小さく汎用でテスト済み | 通常 PR |

local patch の正本は別 checkout 内の Git branch / commit とする。
別枠の mutable patch 履歴はデフォルトでは作らない。必要な handoff は
`notes/YYYY-MM-DD.md` に残し、長期化する場合だけ
`notes/reports/runops-local-patches.md` のような project-local index を作る。

project 側の生成物をそのまま upstream しない。汎用化する場合は source
template / command / docs に戻す:

- `.agents/skills/*` / `.claude/skills/*` → runops source checkout の `src/runops/templates/skills/`
- `AGENTS.md` / `CLAUDE.md` → runops source checkout の `src/runops/templates/harness/`
- `research/*` scaffold → runops source checkout の `src/runops/templates/scaffold/research/`

{{ rule_heading | default("#") }}# しないこと

- ユーザーに相談せずに issue や PR を作らない
- 未サニタイズの HarnessOps feedback を issue / PR / 公開文書に貼らない
- `harness-feedback/records/` を手で作成・移動・書換えしない
- 同じ内容の issue が既にあるか確認せずに重複 issue を切らない
  (`gh issue list --repo Nkzono99/runops --search "<keyword>"` で検索)
- プロジェクト固有の private 情報 (実データパス, クラスタ固有の秘密など)
  を issue 本文に貼らない
- フィードバックを **理由にして現プロジェクトの作業を止めない** — issue や
  PR は作業のサイドチャネルとして投げ、当面の研究タスクは workaround で
  進める
