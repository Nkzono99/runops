{{ rule_heading | default("#") }} Simulator Cookbook ルール

local reference mirror (`refs/`) のシミュレータリポジトリに `cookbook/` がある場合だけ、
入力例やフラグメントをパラメータ生成の補助情報として使う。まず plugin skill、
enabled knowledge、`materials/` を確認し、`refs/` は任意の fallback として扱う。

{{ rule_heading | default("#") }}# 読取順序

1. `refs/<repo>/cookbook/index.toml` が存在するか確認し、`tags` / `status` から候補を絞る
2. 候補 entry の `meta.toml` で `[applicability]` と `[recommended]` を確認
3. `input.toml` の実ファイルを読む
4. fragment を使うときは `[merge]` と `[compatibility]` を確認してから合成

{{ rule_heading | default("#") }}# 注意

- `status = "stable"` の entry を優先する
- fragment の `[compatibility].requires_tags` を entry の tags と照合する
- `[edit_policy]` の `immutable` パラメータは変更しない
- `[cost]` を参考に計算資源を見積もる
